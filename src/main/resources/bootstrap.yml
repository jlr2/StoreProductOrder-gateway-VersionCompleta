spring:
  application:
    name: gateway
  cloud:
    config:
      name: gateway
      uri: http://localhost:8088
      retry:
        max-attempts: 10
        initial-interval: 5000
      fail-fast: true
  profiles:
    active: local